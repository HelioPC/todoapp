export interface ThemeProps {
    theme: {
        name: string;
        colors: {
            primary: string;
            mainBg: string;
            sideBarBg: string;
            sideBarLabelBg: string;
            textPrimary: string;
            textSecundary: string;
            active: string;
        };
    };
}
